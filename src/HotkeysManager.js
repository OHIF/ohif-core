import { HotkeysManager } from './classes/HotkeysManager.js';

// Create commands namespace using a HotkeysManager class instance
const hotkeys = new HotkeysManager();

// Export relevant objects
export default hotkeys;
